/* eslint-disable @typescript-eslint/no-unsafe-call */
import { Inject } from '@nestjs/common'
import { Resolver } from '@nestjs/graphql'

import { {{properCase name}} } from '../generated/graphql'
import { {{properCase name}}Service } from '../services/{{snakeCase name}}.service'

@Resolver('{{properCase name}}')
export class {{properCase name}}Resolver {
  constructor(
    @Inject({{properCase name}}Service)
    private {{snakeCase name}}Service: {{properCase name}}Service
  ) {}
}
